# ML Spec

No ML model is production-ready yet.

Required before any ML phase can pass:

- temporal train/test split;
- no future leakage;
- baseline comparison;
- reproducible training;
- versioned feature schema;
- calibration/evaluation metrics;
- documented weak-performance constraints if a model cannot beat a baseline.

Authoritative financial values must come from deterministic finance state, not ML.
