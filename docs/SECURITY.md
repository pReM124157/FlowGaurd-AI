# Security

Security requirements:

- authentication;
- tenant isolation;
- RBAC;
- input validation;
- secure secrets;
- rate limiting;
- safe errors;
- audit log;
- CSRF protection where relevant;
- no secrets client-side;
- prompt-injection resistance for agent flows.

Prototype policy:

- no autonomous external money movement;
- recommendations only unless an explicit sandbox action is implemented.
